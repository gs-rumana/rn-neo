---
title: Installation
description: Install RN Neo package to your React Native project
---

# Installation

> From `v4` of `react-native-reanimated`, you need to install `react-native-worklets` as a peer dependency.

- Run the following commands in your project root directory:

<div class="tabs tabs-border">
  <input type="radio" name="pkm" class="tab" aria-label="npm" />
  <div class="tab-content">
    ```bash
    npm install rn-neo react-native-reanimated react-native-worklets
    ```
  </div>

  <input
    type="radio"
    name="pkm"
    class="tab"
    aria-label="yarn"
    checked="checked"
  />
  <div class="tab-content">
    ```bash
    yarn add rn-neo react-native-reanimated react-native-worklets
    ```
  </div>

  <input type="radio" name="pkm" class="tab" aria-label="pnpm" />
  <div class="tab-content">
    ```bash
    pnpm add rn-neo react-native-reanimated react-native-worklets
    ```
  </div>

  <input type="radio" name="pkm" class="tab" aria-label="expo" />
  <div class="tab-content">
    ```bash
    expo install rn-neo react-native-reanimated react-native-worklets
    ```
  </div>
</div>

### Expo

- If you are using Expo, you need to add the following to your `app.json` file:

```json
{
  "expo": {
    "plugins": [
      // ...other plugins,
      "rn-neo"
    ]
  }
}
```

- Then run `expo prebuild` to generate the native code.

### React Native CLI

- If you are using React Native CLI project, you need to run the following command in your terminal:

```base
npx react-native-asset
```

- This will link necessary assets for RN Neo theme.

<div className="grid grid-cols-2 gap-4 mt-8">
  <a href="/docs/introduction" className="btn btn-outline btn-lg">
    {' '}
    ❮ &nbsp; Introduction
  </a>
  <a href="/docs/usage" className="btn btn-primary btn-lg">
    Usage &nbsp; ❯
  </a>
</div>
